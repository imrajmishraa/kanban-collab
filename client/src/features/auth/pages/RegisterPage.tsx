/**
 * Placeholder — Register Page
 * Replace this with the real RegisterForm component (Phase 2).
 */
export function RegisterPage() {
  return (
    <div className="flex min-h-screen items-center justify-center" style={{ background: 'var(--bg-root)' }}>
      <div className="text-center">
        <h1 className="text-3xl font-semibold mb-2" style={{ color: 'var(--text-primary)' }}>
          Create account
        </h1>
        <p style={{ color: 'var(--text-secondary)' }}>Register page — coming in Phase 2</p>
      </div>
    </div>
  )
}
